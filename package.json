{
  "name": "intention-builder",
  "version": "0.0.2",
  "description": "Build intenstion to use in browser with es6 modules",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "millerrabin",
  "license": "ISC",
  "dependencies": {
    "mkdirp-promise": "^5.0.1",
    "recursive-readdir": "^2.2.2",
    "rimraf": "^2.6.3"
  }
}
